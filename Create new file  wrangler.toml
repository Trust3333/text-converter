name = "text-converter"
main = "worker.js"
compatibility_date = "2026-04-25"
compatibility_flags = ["nodejs_compat"]

# 关键修复：明确告诉 Wrangler 这是一个纯 Worker（ES Modules）
build.upload.format = "modules"

[observability]
enabled = true
