name = "text-converter"
main = "worker.js"
compatibility_date = "2026-04-25"
compatibility_flags = ["nodejs_compat"]

# 强制指定为纯 Worker（解决 static files 检测问题）
[build.upload]
format = "modules"

[observability]
enabled = true
